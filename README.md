# golang-psbigpicure-code

Very simple examples from the PS Big Picture course.

To init a module:

```sh
go mod init demo
```

Running the web server:

```sh
go run .
```

Running the CLI:

```sh
go run . -level DEBUG
```
